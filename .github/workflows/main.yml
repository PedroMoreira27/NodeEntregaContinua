name: Deploy Application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Log into DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/minha-aplicacao:${{ github.sha }} .
          docker push ${{ secrets.DOCKER_USERNAME }}/minha-aplicacao:${{ github.sha }}

      - name: Deploy on Server
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: 201.23.3.86
          username: aluno
          key: ${{ secrets.SSH_KEY }}
          port: 22
          script: |
            # Garantir que o Docker está rodando corretamente
            docker --version
            
            # Baixar a última imagem da aplicação
            docker pull ${{ secrets.DOCKER_USERNAME }}/minha-aplicacao:${{ github.sha }}
            
            # Parar e remover qualquer container existente
            docker stop app-node || true
            docker rm app-node || true

            # Rodar a aplicação na porta 8100
            docker run -d --name app-node -p 8100:8100 ${{ secrets.DOCKER_USERNAME }}/minha-aplicacao:${{ github.sha }}

            # Verificar se o container está rodando
            docker ps

            # Verificar logs do container para garantir que não houve erro
            docker logs app-node
